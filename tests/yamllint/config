extends: default

rules:
  document-start: disable
  braces: { min-spaces-inside: 1, max-spaces-inside: 2 }
  brackets: { min-spaces-inside: 1, max-spaces-inside: 2 }
  colons: { level: warning }
  line-length: { level: warning }